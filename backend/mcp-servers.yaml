servers:
  - name: text
    url: ${MCP_TEXT_URL:-http://localhost:8001/mcp}
    enabled: true

  - name: numbers
    url: ${MCP_NUMBERS_URL:-http://localhost:8002/mcp}
    enabled: true

  - name: demo-app
    url: ${MCP_DEMO_APP_URL:-http://localhost:8003/mcp}
    enabled: true
